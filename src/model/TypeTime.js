if (typeof define !== 'function') {var define = require('amdefine')(module) }

define('model/TypeTime', [], function() {

  var TypeTime = function() {
  };

  TypeTime.getEntries = function(attr) {

  };

  return TypeTime;

});

